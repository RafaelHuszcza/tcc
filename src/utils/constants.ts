export const ROUTES = {
  PUBLIC_NEEDS: '/needs',
  PUBLIC_SHELTERS: '/',
  NOT_FOUND: '/404',
}

export const API_ROUTES = {
  PUBLIC_NEEDS: '/public-needs',
  PUBLIC_SHELTERS: '/public-shelters',
  SHELTERS: '/shelters',
  USERS: '/users',
}
